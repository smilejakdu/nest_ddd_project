version: '3.7'
services:
  nginx:
    environment:
      - TZ=Asia/Seoul
    build:
      context: .
      dockerfile: ./compose/local/nginx/Dockerfile
    container_name: nest_ddd_nginx
    depends_on:
      - nest
      - db
    ports:
      - "80:80"
    links:
      - nest

  nest:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      - TZ=Asia/Seoul
    container_name: nest_ddd_backend
    ports:
      - '8000:8000'
    env_file:
      - compose/local/dev.env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    links:
      - db
    depends_on:
      - db

  db:
    environment:
      - TZ=Asia/Seoul
    image: mysql:8.0.23
    restart: unless-stopped
    container_name: nest_ddd_mysql
    platform: linux/x86_64
    ports:
      - '3306:3306'
    volumes:
      - ./mysql/mysql_data:/var/lib/mysql
      - ./mysql/sqls/:/docker-entrypoint-initdb.d/
    env_file:
      - compose/local/mysql.env
